# REBUILD TRIGGER: 2026-07-26T03:30:00Z - fix pacote chromium (build estava falhando: "chromium-browser" nao existe no Debian, pacote correto e "chromium")
services: [{type: web, name: tdrive-automacao, runtime: node, buildCommand: "apt-get update && apt-get install -y chromium fonts-liberation libnss3 libatk-bridge2.0-0 libgtk-3-0 libgbm1 && (test -e /usr/bin/chromium || ln -s /usr/bin/chromium-browser /usr/bin/chromium || true) && npm ci", startCommand: "npm start", plan: free, envVars: [{key: PUPPETEER_SKIP_DOWNLOAD, value: "true"}, {key: PUPPETEER_EXECUTABLE_PATH, value: "/usr/bin/chromium"}]}]
